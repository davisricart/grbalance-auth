// public-config.js - This file is safe to commit to GitHub
// Contains NO sensitive API keys or credentials

// Project IDs are not considered sensitive
const PROJECT_ID = "gr-balance";

// This tells your app how to find the full configuration
const CONFIG_SOURCE = "firebase-remote-config";  // Options: firebase-remote-config, local-file